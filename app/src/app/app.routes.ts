import { Component } from '@angular/core';
import { Routes } from '@angular/router';
import { Aviso } from './components/aviso/aviso';

export const routes: Routes = [
  {path: "avisos", component: Aviso}
];
